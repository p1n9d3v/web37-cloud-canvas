{
    "name": "@cloud-canvas/server",
    "version": "0.0.0",
    "description": "",
    "private": true,
    "keywords": [],
    "scripts": {
        "start": "cross-env NODE_ENV=development node dist/index.js",
        "dev": "cross-env NODE_ENV=development tsx watch src/index.ts",
        "build": "tsup --config tsup.build.config.ts",
        "bundle": "tsup --config tsup.bundle.config.ts",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "chalk": "^5.3.0",
        "express": "^4.21.1"
    },
    "devDependencies": {
        "@types/express": "^5.0.0",
        "@types/node": "^22.8.2",
        "cross-env": "^7.0.3",
        "tsup": "^8.3.5",
        "tsx": "^4.19.2"
    }
}
